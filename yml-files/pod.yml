apiVersion: v1
kind: Pod
metadata:
 name: helloworld-pod
 namespace: default
 labels:
  app: helloworld
spec:
 containers:
  - name: helloworld
    image: christianhxc/helloworld:1.0
    ports:
      - containerPort: 80
    resources:
      requests:
        cpu: 50m
      limits:
        cpu: 100m